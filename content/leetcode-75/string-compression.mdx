---
title: "443. String Compression"
order: 9
description: "用雙指針原地壓縮字元陣列記錄字元和出現次數"
---

# String Compression

<ELI5>
想像你在整理一排重複的積木：紅紅紅藍藍綠。與其說「紅紅紅藍藍綠」，你可以簡寫成「紅3藍2綠」— 每種顏色後面加上出現次數（只有 1 個就不寫數字）。

這題要你在原地（不用額外陣列）做這件事。用一個「讀取指針」數連續相同字元，用一個「寫入指針」把壓縮結果寫回原陣列。
</ELI5>

## 題目描述

給定字元陣列 `chars`，原地壓縮。連續相同字元替換為「字元+計數」（計數為 1 時省略）。回傳壓縮後陣列的長度。

```
輸入：chars = ["a","a","b","b","c","c","c"]
輸出：6（陣列變為 ["a","2","b","2","c","3"]）

輸入：chars = ["a"]
輸出：1

輸入：chars = ["a","b","b","b","b","b","b","b","b","b","b","b","b"]
輸出：4（陣列變為 ["a","b","1","2"]）
```

## 解題思路

<StepDiagram data='[{"title":"初始化","description":"write = 0（下一個寫入位置），read = 0（當前讀取位置）","icon":"1"},{"title":"計算連續字元","description":"內層 while 迴圈：從 read 開始，當 chars[read] 等於當前字元時 read++ 和 count++","icon":"2"},{"title":"寫入字元","description":"把當前字元寫入 chars[write]，write 前進一格","icon":"3"},{"title":"寫入計數","description":"若 count > 1，把 count 轉為字串（如 12 → \"1\",\"2\"），逐字元寫入 chars[write++]","icon":"4"},{"title":"繼續","description":"read 已指向下一組不同字元的起點，回到步驟 2 重複直到 read 到底","icon":"5"}]' />

## 程式碼

### C#

```csharp
public class Solution {
    public int Compress(char[] chars) {
        int write = 0, read = 0;
        while (read < chars.Length) {
            char current = chars[read];
            int count = 0;
            // 計算連續相同字元數
            while (read < chars.Length && chars[read] == current) {
                read++;
                count++;
            }
            // 寫入字元
            chars[write++] = current;
            // 寫入計數（大於 1 時）
            if (count > 1) {
                foreach (char c in count.ToString())
                    chars[write++] = c;
            }
        }
        return write;
    }
}
```

### TypeScript

```typescript
function compress(chars: string[]): number {
    let write = 0, read = 0;
    while (read < chars.length) {
        const current = chars[read];
        let count = 0;
        while (read < chars.length && chars[read] === current) {
            read++;
            count++;
        }
        chars[write++] = current;
        if (count > 1) {
            for (const c of count.toString()) {
                chars[write++] = c;
            }
        }
    }
    return write;
}
```

### Python

```python
def compress(chars: list[str]) -> int:
    write = read = 0
    while read < len(chars):
        current = chars[read]
        count = 0
        while read < len(chars) and chars[read] == current:
            read += 1
            count += 1
        chars[write] = current
        write += 1
        if count > 1:
            for c in str(count):
                chars[write] = c
                write += 1
    return write
```

## 複雜度分析

| | Time | Space |
|--|------|-------|
| 本解法 | O(n) | O(1) |

read 指針從頭到尾掃描一遍 O(n)，write 指針最多寫 n 個字元。原地修改不需額外陣列，只用 write、read、count 等常數變數，空間 O(1)。

## 邊界情況

- **每個字元都不重複**：例如 `["a","b","c"]`，每組 count=1 不寫數字，壓縮後長度不變
- **只有一個字元**：例如 `["a"]`，回傳長度 1
- **連續超過 9 個**：例如 12 個 b，需要寫入 `"b","1","2"` 三個字元，count.toString() 產生多位數字

## 圖解

<Diagram data='{"nodes":[{"id":"input","label":"[a,a,b,b,c,c,c]","type":"default"},{"id":"step1","label":"寫入 a,2","type":"highlight"},{"id":"step2","label":"寫入 b,2","type":"highlight"},{"id":"step3","label":"寫入 c,3","type":"highlight"},{"id":"result","label":"[a,2,b,2,c,3] len=6","type":"muted"}],"edges":[{"from":"input","to":"step1","label":"read 掃描 aa"},{"from":"step1","to":"step2","label":"read 掃描 bb"},{"from":"step2","to":"step3","label":"read 掃描 ccc"},{"from":"step3","to":"result","label":"完成"}]}' />

圖表展示三組壓縮：read 先掃描兩個 a（count=2），write 寫入 `a,2`；接著掃描兩個 b，寫入 `b,2`；最後三個 c，寫入 `c,3`。原陣列長度 7 壓縮為 6。

## 進階觀點

<Callout type="tip" title="面試追問">
- **計數超過 9 怎麼處理？** 把 count 轉為字串後逐字元寫入。例如 count=12 轉為 `"12"`，寫入 `'1'` 和 `'2'` 兩個字元。C# 用 `count.ToString()`，Python 用 `str(count)`。
- **為什麼寫入指針不會超過讀取指針？** 一個字元出現 n 次，壓縮後佔 1+len(str(n)) 格。當 n=1 時佔 1 格（省略計數），n ≥ 2 時 1+digits(n) ≤ n（例如 n=2 佔 2 格，n=10 佔 3 格）。所以壓縮後長度永遠 ≤ 原始長度。
- **Run-Length Encoding 的應用？** BMP 影像格式、傳真機資料傳輸、遊戲地圖壓縮都用 RLE。它對連續重複資料效果極佳，但對隨機資料可能反而膨脹。
</Callout>

## 理解測驗

<Quiz data='{"question":"為什麼 write 指針永遠不會超過 read 指針？","options":["因為陣列是排序的","因為壓縮後表示永遠不比原始長（1個字元寫1位，n>1個字元寫1+digits位，而 n >= 1+digits）","因為用了額外陣列","因為只處理小寫字母"],"answer":1,"explanation":"即使最差情況（每個字元都不同），write 也只是跟 read 同步，不會超過。"}' />

<Quiz data='{"question":"chars = [\"a\",\"b\",\"c\"] 壓縮後結果是什麼？","options":["[a,1,b,1,c,1] 長度6","[a,b,c] 長度3","[abc] 長度1","[a,b,c,3] 長度4"],"answer":1,"explanation":"每個字元只出現 1 次，計數為 1 時不寫數字，所以陣列不變。"}' />
