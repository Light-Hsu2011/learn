---
title: "2300. Successful Pairs of Spells and Potions"
order: 54
description: "排序藥水陣列後用二分搜尋找出每個法術的成功配對數"
---

# Successful Pairs of Spells and Potions

<ELI5>
想像你是一個魔法師，有一堆法術和一堆藥水。每個法術和藥水搭配會產生一個「威力值」（兩者相乘），只有威力值達到門檻才算「成功配對」。

要怎麼快速知道每個法術能配對幾瓶藥水？先把藥水從弱到強排好，然後對每個法術，用二分搜尋找到「剛好夠強的藥水」在哪裡——從那裡到最後的藥水都能成功配對。就像在書架上找到某本書的位置，後面的書都比它厚。
</ELI5>

## 題目描述

給定 `spells` 和 `potions` 兩個陣列，以及一個整數 `success`。當 `spells[i] * potions[j] >= success` 時為成功配對。回傳一個陣列，其中第 i 個元素是第 i 個法術的成功配對數。

```
輸入：spells = [5,1,3], potions = [1,2,3,4,5], success = 7
輸出：[4,0,3]
解釋：spell=5 配對 [2,3,4,5]→4個, spell=1 配對 0個, spell=3 配對 [3,4,5]→3個
```

## 解題思路

<StepDiagram data='[{"title":"排序藥水","description":"將 potions 升序排列","icon":"1"},{"title":"計算門檻","description":"對每個 spell，最低需要的藥水強度 = ceil(success / spell)","icon":"2"},{"title":"二分搜尋","description":"在排序後的 potions 中找第一個 >= 門檻的位置","icon":"3"},{"title":"計算配對數","description":"成功配對數 = potions.length - 找到的位置","icon":"4"}]' />

## 程式碼

### C#
```csharp
public class Solution {
    public int[] SuccessfulPairs(int[] spells, int[] potions, long success) {
        Array.Sort(potions);
        int n = potions.Length;
        int[] result = new int[spells.Length];
        
        for (int i = 0; i < spells.Length; i++) {
            // 最低需要的藥水強度
            long minPotion = (success + spells[i] - 1) / spells[i]; // ceil division
            // 二分搜尋第一個 >= minPotion 的位置
            int left = 0, right = n;
            while (left < right) {
                int mid = left + (right - left) / 2;
                if (potions[mid] >= minPotion) right = mid;
                else left = mid + 1;
            }
            result[i] = n - left;
        }
        return result;
    }
}
```

### TypeScript
```typescript
function successfulPairs(spells: number[], potions: number[], success: number): number[] {
    potions.sort((a, b) => a - b);
    const n = potions.length;
    
    return spells.map(spell => {
        const minPotion = Math.ceil(success / spell);
        // 二分搜尋第一個 >= minPotion 的位置
        let left = 0, right = n;
        while (left < right) {
            const mid = Math.floor((left + right) / 2);
            if (potions[mid] >= minPotion) right = mid;
            else left = mid + 1;
        }
        return n - left;
    });
}
```

### Python
```python
import bisect
import math

def successfulPairs(spells: list[int], potions: list[int], success: int) -> list[int]:
    potions.sort()
    n = len(potions)
    result = []
    
    for spell in spells:
        min_potion = math.ceil(success / spell)
        # bisect_left 找第一個 >= min_potion 的位置
        idx = bisect.bisect_left(potions, min_potion)
        result.append(n - idx)
    
    return result
```

## 複雜度分析

| | Time | Space |
|--|------|-------|
| 本解法 | O((m + n) log n) | O(1)（不計輸出） |

其中 m = spells 長度，n = potions 長度。

## 圖解

<Diagram data='{"nodes":[{"id":"sort","label":"排序 potions","type":"highlight"},{"id":"spell","label":"遍歷每個 spell","type":"default"},{"id":"threshold","label":"計算門檻值","type":"default"},{"id":"bsearch","label":"Binary Search 找位置","type":"highlight"},{"id":"count","label":"n - pos = 成功配對數","type":"muted"}],"edges":[{"from":"sort","to":"spell","label":"排序後"},{"from":"spell","to":"threshold","label":"ceil(success/spell)"},{"from":"threshold","to":"bsearch","label":"在 potions 中找"},{"from":"bsearch","to":"count","label":"計算數量"}]}' />

## 進階觀點

<Callout type="tip" title="面試追問">
- 為什麼要用 `ceil(success / spell)` 而不是 `success / spell`？因為需要乘積 ≥ success，所以門檻要向上取整。
- 如果 spells 也排序，可以用雙指標進一步優化嗎？可以，但需要額外處理原始順序的對應關係。
- 這題的核心模式是「排序 + 二分搜尋」，類似的題目包括：計算比某值大的元素數量、區間查詢等。
</Callout>

## 理解測驗

<Quiz data='{"question":"為什麼先排序 potions 再用二分搜尋，而不是對每個 spell 暴力遍歷 potions？","options":["因為題目要求排序","暴力法 O(m×n) 太慢，排序+二分 O((m+n)logn) 更快","排序後答案會不同","二分搜尋不需要排序"],"answer":1,"explanation":"暴力法對每個 spell 遍歷所有 potions 是 O(m×n)，排序一次 O(n log n) 後每次二分 O(log n)，總共 O((m+n) log n) 快很多。"}' />

<Quiz data='{"question":"如果某個 spell 非常大，使得所有 potions 都能成功配對，二分搜尋會返回什麼位置？","options":["返回 n（陣列長度）","返回 0","返回 -1","返回中間位置"],"answer":1,"explanation":"所有 potions 都 >= 門檻時，二分搜尋找到的位置是 0，所以配對數 = n - 0 = n，即全部配對成功。"}' />
