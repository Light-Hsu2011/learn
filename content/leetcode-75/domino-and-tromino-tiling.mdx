---
title: "790. Domino and Tromino Tiling"
order: 62
description: "狀態轉移方程推導 2×N 棋盤用 Domino 和 Tromino 的鋪法數"
difficulty: "hard"
interviewImportance: 2
related:
  - "leetcode-75/unique-paths"
  - "leetcode-75/n-th-tribonacci-number"
---

# Domino and Tromino Tiling

<ELI5>
想像你有一個 2 行 N 列的長條地板，你要用兩種磁磚鋪滿它：
- Domino（骨牌）：2×1 的長方形，可以橫放或直放
- Tromino（L 形磁磚）：佔 3 格的 L 形，可以旋轉成 4 個方向

問題是：有幾種不同的鋪法？

這就像拼拼圖：每次你放一塊磁磚，地板的「剩餘形狀」會改變。我們用狀態來記錄每一列的填充情況，然後推導出從一個狀態到下一個狀態的轉移關係。
</ELI5>

## 題目描述

用 Domino（2×1）和 Tromino（L 形）鋪滿 2×n 的棋盤，回傳方法數（mod 10^9+7）。

```
輸入：n = 3
輸出：5

輸入：n = 1
輸出：1
```

## 解題思路

<StepDiagram data='[{"title":"觀察規律","description":"手動列舉小 case：n=1→1, n=2→2, n=3→5, n=4→11。嘗試找出遞推關係","icon":"1"},{"title":"定義狀態","description":"f(n) = 完整填滿 2×n 棋盤的方法數。注意是「完全填滿」不是「部分填滿」","icon":"2"},{"title":"推導遞推式","description":"f(n) = 2×f(n-1) + f(n-3)。2×f(n-1) 來自 Tromino 兩個方向各延伸 f(n-1)，f(n-3) 來自純 Domino 組合","icon":"3"},{"title":"滾動計算","description":"f(n) 只依賴 f(n-1) 和 f(n-3)，可用三個變數滾動計算。也可直接用陣列更直觀","icon":"4"},{"title":"取模","description":"每步對 10^9+7 取模，防止乘法中間結果溢位 64 位整數","icon":"5"}]' />

## 程式碼

### C#
```csharp
public class Solution {
    public int NumTilings(int n) {
        const int MOD = 1_000_000_007;
        if (n <= 2) return n;
        
        long[] dp = new long[n + 1];
        dp[0] = 1; dp[1] = 1; dp[2] = 2;
        
        for (int i = 3; i <= n; i++) {
            // f(n) = 2*f(n-1) + f(n-3)
            dp[i] = (2 * dp[i - 1] + dp[i - 3]) % MOD;
        }
        return (int)dp[n];
    }
}
```

### TypeScript
```typescript
function numTilings(n: number): number {
    const MOD = 1_000_000_007;
    if (n <= 2) return n;
    
    const dp = new Array(n + 1).fill(0);
    dp[0] = 1; dp[1] = 1; dp[2] = 2;
    
    for (let i = 3; i <= n; i++) {
        dp[i] = (2 * dp[i - 1] + dp[i - 3]) % MOD;
    }
    return dp[n];
}
```

### Python
```python
def numTilings(n: int) -> int:
    MOD = 10**9 + 7
    if n <= 2:
        return n
    
    dp = [0] * (n + 1)
    dp[0], dp[1], dp[2] = 1, 1, 2
    
    for i in range(3, n + 1):
        dp[i] = (2 * dp[i - 1] + dp[i - 3]) % MOD
    
    return dp[n]
```

## 複雜度分析

| | Time | Space |
|--|------|-------|
| 本解法 | O(n) | O(n)（可優化為 O(1)） |

- **Time**：從 3 到 n 線性遍歷，每步只做一次乘法、一次加法和一次取模
- **Space**：用長度 n+1 的陣列存中間結果。若改用三個滾動變數可降到 O(1)

## 圖解

<Diagram data='{"nodes":[{"id":"n1","label":"n=1: 1種","type":"muted"},{"id":"n2","label":"n=2: 2種","type":"muted"},{"id":"n3","label":"n=3: 5種","type":"highlight"},{"id":"n4","label":"n=4: 11種","type":"highlight"},{"id":"formula","label":"f(n) = 2f(n-1) + f(n-3)","type":"default"}],"edges":[{"from":"n1","to":"n3","label":"f(n-2)→間接"},{"from":"n2","to":"n3","label":"f(n-1)×2"},{"from":"n3","to":"n4","label":"f(n-1)×2"},{"from":"n1","to":"n4","label":"f(n-3)"},{"from":"formula","to":"n4","label":"公式"}]}' />

圖表展示了遞推關係：f(4)=11 來自 2×f(3) + f(1) = 2×5 + 1 = 11。
公式中 2×f(n-1) 對應 Tromino 上下兩個方向延伸的鋪法，f(n-3) 對應三個直立 Domino 的組合。

## 進階觀點

<Callout type="tip" title="面試追問">
- **遞推式 f(n) = 2×f(n-1) + f(n-3) 怎麼推導？** 定義兩個狀態：f(n) 是完整填滿 2×n 的方法數，g(n) 是 2×n 加一個突出格的方法數。轉移：f(n) = f(n-1) + f(n-2) + 2×g(n-1)，g(n) = g(n-1) + f(n-2)。消去 g(n) 後化簡得到 f(n) = 2×f(n-1) + f(n-3)。
- **用兩個 DP 陣列的做法？** 定義 full[n]（完整填滿）和 partial[n]（有一格突出），轉移更直觀：full[n] = full[n-1] + full[n-2] + 2×partial[n-1]，partial[n] = partial[n-1] + full[n-2]。
- **n 極大時怎麼辦？** f(n) 的遞推涉及 f(n-1)、f(n-3)，可以建構 3×3 轉移矩陣，用矩陣快速冪在 O(log n) 內計算。
</Callout>

## 邊界情況

- **n 等於 1**：只能放一塊直立 Domino，回傳 1
- **n 等於 2**：兩塊直立或兩塊橫放，共 2 種，回傳 2
- **n 等於 1000（題目上限）**：結果是超大數字，必須每步取模避免溢位。注意 C# 中 2×dp[i-1] 可能超過 int 範圍，建議用 long

## 理解測驗

<Quiz data='{"question":"f(n) = 2*f(n-1) + f(n-3) 中的 2*f(n-1) 代表什麼？","options":["放兩塊 Domino","對應在最後一列放一個直立 Domino 和 Tromino 的兩個方向","隨機乘以 2","n-1 的方法數的兩倍是因為有上下對稱"],"answer":1,"explanation":"係數 2 來自於 Tromino 有上下兩個方向可以突出到最後一列，每種方向各貢獻了一部分 f(n-1) 的延伸方式。"}' />

<Quiz data='{"question":"為什麼需要對 10^9+7 取模？","options":["讓答案更小方便閱讀","因為方法數增長非常快，超出整數範圍，取模是演算法競賽慣例","為了讓 DP 更快","題目沒有要求取模"],"answer":1,"explanation":"鋪法數隨 n 指數增長，很快就會超出 64 位整數範圍。取模 10^9+7（一個大質數）是演算法題的標準做法。"}' />
