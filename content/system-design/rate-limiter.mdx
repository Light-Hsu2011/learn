---
title: "設計限流器"
order: 5
description: "保護系統不被流量淹沒，常見限流演算法與分散式實作"
---

# 設計限流器（Rate Limiter）

<ELI5>
想像一家超人氣拉麵店，一次只能坐 20 人：

- **Token Bucket**：門口有個桶，每秒放入 5 個號碼牌。來的客人拿到牌就進去，桶空了就要等。桶最多存 20 個牌，所以允許短暫的排隊爆量
- **Sliding Window**：店員看最近 1 分鐘內進了幾個人。超過 60 人就不讓進，不管你是什麼時候來的

限流器就是這個門口管理員 — 保護店（系統）不被人潮（流量）擠爆，讓每個客人都有好的體驗。
</ELI5>

## 問題定義

設計一個 Rate Limiter，控制用戶或服務在時間窗口內的請求數量，保護後端服務不被過量流量壓垮。

<Callout type="info" title="限流的位置">
Rate Limiter 通常放在 **API Gateway** 或 **Middleware** 層，在請求到達業務邏輯之前就做判斷。返回 **HTTP 429 Too Many Requests** 表示被限流。
</Callout>

## 需求分析

- **功能性需求**：按用戶/IP/API key 限流、可配置限流規則（每秒/每分鐘 N 次）、超過限制返回 429
- **非功能性需求**：低延遲（限流判斷 < 1ms）、高可用、分散式環境下準確
- **限流粒度**：用戶級、IP 級、API 級、全局級
- **軟限流 vs 硬限流**：軟限流允許短暫超額，硬限流嚴格拒絕

## 注意事項

<Callout type="warning" title="分散式環境的挑戰">
單機限流很簡單（記憶體計數器就好），但多台伺服器時，每台的計數器是獨立的。用戶的請求被分散到不同伺服器，每台都覺得沒超限，實際上總量早就超了。必須用 Redis 等集中式儲存來做分散式限流。
</Callout>

## 設計流程

<StepDiagram data='[{"title":"選擇限流演算法","description":"Token Bucket、Leaky Bucket、Fixed Window、Sliding Window","icon":"1"},{"title":"決定限流粒度","description":"按用戶 ID、IP、API Key 或全局","icon":"2"},{"title":"設定限流規則","description":"定義每個粒度的速率限制（如每秒 10 次）","icon":"3"},{"title":"選擇儲存方案","description":"單機用記憶體，分散式用 Redis","icon":"4"},{"title":"整合到 API Gateway","description":"作為 Middleware 在請求處理前攔截","icon":"5"},{"title":"回應 Header 設計","description":"返回 X-RateLimit-Remaining 等資訊","icon":"6"}]' />

## 架構設計

### Token Bucket 演算法（C#）

```csharp
public class TokenBucket
{
    private readonly int _capacity;       // 桶的最大容量
    private readonly double _refillRate;  // 每秒填充幾個 token
    private double _tokens;
    private DateTime _lastRefill;
    private readonly object _lock = new();

    public TokenBucket(int capacity, double refillRate)
    {
        _capacity = capacity;
        _refillRate = refillRate;
        _tokens = capacity;
        _lastRefill = DateTime.UtcNow;
    }

    public bool TryConsume(int tokens = 1)
    {
        lock (_lock)
        {
            Refill();
            if (_tokens < tokens) return false;
            _tokens -= tokens;
            return true;
        }
    }

    private void Refill()
    {
        var now = DateTime.UtcNow;
        var elapsed = (now - _lastRefill).TotalSeconds;
        _tokens = Math.Min(_capacity, _tokens + elapsed * _refillRate);
        _lastRefill = now;
    }
}
```

### 分散式 Sliding Window（Redis + TypeScript）

```typescript
async function isAllowed(
  userId: string,
  windowMs: number,
  maxRequests: number
): Promise<boolean> {
  const now = Date.now();
  const key = `ratelimit:${userId}`;

  // Redis pipeline: 原子操作
  const pipeline = redis.pipeline();
  pipeline.zremrangebyscore(key, 0, now - windowMs); // 移除過期紀錄
  pipeline.zadd(key, now, `${now}:${Math.random()}`); // 加入當前請求
  pipeline.zcard(key); // 計算窗口內請求數
  pipeline.expire(key, Math.ceil(windowMs / 1000)); // 設定 key 過期

  const results = await pipeline.exec();
  const count = results![2][1] as number;
  return count <= maxRequests;
}

// Middleware
app.use(async (req, res, next) => {
  const allowed = await isAllowed(req.userId, 60_000, 100); // 每分鐘 100 次
  if (!allowed) {
    res.set("Retry-After", "60");
    res.status(429).json({ error: "Rate limit exceeded" });
    return;
  }
  next();
});
```

## 架構圖

<Diagram data='{"nodes":[{"id":"client","label":"Client","type":"muted"},{"id":"gateway","label":"API Gateway","type":"default"},{"id":"limiter","label":"Rate Limiter (Middleware)","type":"highlight"},{"id":"redis","label":"Redis (Counters)","type":"highlight"},{"id":"rules","label":"Rules Config","type":"default"},{"id":"api","label":"Backend API","type":"default"}],"edges":[{"from":"client","to":"gateway","label":"request"},{"from":"gateway","to":"limiter","label":"check rate"},{"from":"limiter","to":"redis","label":"read/update counter"},{"from":"limiter","to":"rules","label":"load rules"},{"from":"limiter","to":"api","label":"allowed → forward"},{"from":"limiter","to":"client","label":"denied → 429"}]}' />

## 實戰補充

<Callout type="tip" title="四種演算法比較">
- **Token Bucket**：允許短暫的突發流量（桶裡的 token 可以一次用完），最常用
- **Leaky Bucket**：固定速率處理請求，多餘的排隊或丟棄，適合需要平滑流量的場景
- **Fixed Window Counter**：簡單但有邊界問題（窗口交界處可能有兩倍流量）
- **Sliding Window Log**：最精確但記憶體用量大（記錄每個請求的時間戳）
- **Sliding Window Counter**：結合 Fixed Window 和 Sliding Window Log 的優點，用加權計算
</Callout>

## 理解測驗

<Quiz data='{"question":"為什麼 Fixed Window Counter 有「邊界問題」？","options":["它不能處理分散式環境","在兩個窗口交界處（如 0:59 和 1:00），短時間內可能允許兩倍的請求量","它的計數器會溢位","它不支援多用戶"],"answer":1,"explanation":"假設限制每分鐘 100 次。用戶在 0:59 發送 100 次，1:00 又發送 100 次。在 Fixed Window 看來兩個窗口都沒超限，但實際上在 0:59-1:00 這 2 秒內就有 200 次請求。Sliding Window 可以解決這個問題。"}' />

<Quiz data='{"question":"在分散式環境中，為什麼不能只用每台伺服器的本地記憶體做限流？","options":["記憶體太貴","每台伺服器的計數器獨立，無法準確計算用戶的總請求數","本地記憶體的速度太慢","伺服器沒有足夠的記憶體"],"answer":1,"explanation":"Load Balancer 會把同一用戶的請求分到不同伺服器。如果每台各自計數，用戶限制 100 次/分鐘，3 台伺服器每台允許 100 次，實際上用戶可以發 300 次。需要 Redis 等集中式儲存來統一計數。"}' />

<Quiz data='{"question":"Token Bucket 演算法中，「桶的容量」代表什麼意義？","options":["每秒處理的請求數","系統能承受的最大突發流量","資料庫的連線數","快取的大小"],"answer":1,"explanation":"桶的容量決定了允許的最大突發量。如果桶滿了有 100 個 token，用戶可以瞬間發送 100 個請求（把桶清空）。之後就只能按照填充速率（refill rate）持續發送。"}' />

## 重點整理

<Callout type="tip" title="一句話記住">
**Rate Limiter = Token Bucket（允許突發）或 Sliding Window（精確計數）+ Redis 集中式計數，放在 API Gateway 攔截。**
</Callout>

| 概念 | 說明 |
|------|------|
| **Token Bucket** | 允許突發流量，最常用的演算法 |
| **Sliding Window** | 精確計數，沒有邊界問題 |
| **HTTP 429** | Rate Limit 超限的標準回應碼 |
| **Redis** | 分散式限流的集中式計數器 |
| **API Gateway** | Rate Limiter 的最佳部署位置 |
